@startuml

title C4 Level 4 - Code Diagram: POST /forgot-password handler (TODO, planned)

start

:Receive request body\n{ email, recaptchaToken };

if (email missing or invalid format?) then (yes)
  :Return 400\n{ message: "Invalid email" };
  stop
endif

if (recaptchaToken missing or invalid?) then (yes)
  :Return 400\n{ message: "reCAPTCHA verification failed" };
  stop
endif

if (user with email not found?) then (yes)
  :Return 404\n{ message: "User with this email doesn't exist" };
  stop
endif

:Generate password reset token\n(expires after a limited time);

:Save token linked to user;

:Build recovery link\nCLIENT_URL + "/create-new-password?token=" + token;

:Send email via Email Service\n(TODO: provider not selected);

:Return 200\n{ message: "We have sent a link to confirm your email to ___email___" };

stop

@enduml
